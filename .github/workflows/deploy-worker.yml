name: Deploy Cloudflare Worker

on:
  push:
    branches:
      - main
    paths:
      - worker/**
      - .github/workflows/deploy-worker.yml
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Show config
        working-directory: worker
        run: |
          echo "=== wrangler.toml ==="
          cat wrangler.toml
          echo "=== files ==="
          ls -la
          echo "=== syntax check ==="
          node --check index.js

      - name: Verify Cloudflare auth
        working-directory: worker
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: npx wrangler@3.78.12 whoami

      - name: Deploy Worker
        working-directory: worker
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: npx wrangler@3.78.12 deploy
